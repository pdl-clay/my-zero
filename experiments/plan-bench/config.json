{
  "_comment": "Judging is done by hand (Claude reads prompts/judge.txt and scores each saved spec) instead of a zero exec call - step-3.5-flash wasn't reliable enough as an automated judge. Add more entries to models_under_test as more providers/quota become available (e.g. opencode-go once its monthly cap resets).",
  "repo_path": "../..",
  "models_under_test": [
    {"provider": "stepfun-plan", "model": "step-3.7-flash"}
  ],
  "variants": ["mono", "explore_critique", "full"],
  "explorer_count": 2,
  "explorer_focuses": [
    "Existing patterns/conventions directly relevant to this task (similar features, naming, file layout).",
    "Anything that could break or interact badly with this task (adjacent code, tests, config it might affect)."
  ],
  "critic_personas": ["critic_logic", "critic_security"],
  "draft_reasoning_effort": "low",
  "max_turns": {
    "explorer": 40,
    "critic": 35,
    "checker": 40,
    "draft": 40
  },
  "timeout_seconds": 900,
  "parallel_workers": 3
}
